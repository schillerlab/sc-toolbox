```{highlight} shell

```

# Installation

## Stable release

To install sc-toolbox, run this command in your terminal:

```console
$ pip install sc-toolbox
```

This is the preferred method to install sc-toolbox, as it will always install the most recent stable release.

If you don't have [pip] installed, this [Python installation guide] can guide
you through the process.

## From sources

The sources for sc-toolbox can be downloaded from the [Github repo].
Please note that you require [poetry] to be installed.

You can either clone the public repository:

```console
$ git clone git://github.com/schillerlab/sc-toolbox
```

Or download the [tarball]:

```console
$ curl -OJL https://github.com/schillerlab/sc-toolbox/tarball/master
```

Once you have a copy of the source, you can install it with:

```console
$ make install
```

[github repo]: https://github.com/schillerlab/sc-toolbox
[pip]: https://pip.pypa.io
[poetry]: https://python-poetry.org/
[python installation guide]: http://docs.python-guide.org/en/latest/starting/installation/
[tarball]: https://github.com/schillerlab/sc-toolbox/tarball/master
